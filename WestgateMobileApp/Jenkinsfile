pipeline {
    agent any

    options {
        timestamps()
        skipDefaultCheckout(false)
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Sanity') {
            steps {
                echo 'Jenkinsfile parsed successfully'
            }
        }
    }
}
